import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(
  document.getElementById("root"),
).render(
  <div>
    asdhjgasdjkhasgdhjksad
    <h3 className="md:min-w-full text-blue-600 text-xs uppercase font-bold block pt-1 pb-4 no-underline">
      Admin Layout Pages
    </h3>
    <h1 className="text-4xl text-blue-700">My Webpack + Tailwind App</h1>
  </div>
);